; Инкремент десятичного числа
; Пройти по всем символам вправо не меняя их
0 * * r 0

; При достижении пробела возвращаемся на последний символ для проверки
0 _ * l 1

; Все символы [0...8] просто инкрементируем и заканчиваем преобразования
1 0 1 * halt
1 1 2 * halt
1 2 3 * halt
1 3 4 * halt
1 4 5 * halt
1 5 6 * halt
1 6 7 * halt
1 7 8 * halt
1 8 9 * halt

; Символ 9 заменяем на 0, смещаемся влево и повторяем проверку символов
1 9 0 l 1

; Если при выполнении операции 1 был достигнут пробел, значит все символы в числе были 9, тогда заменяем пробел на 1 и заканчиваем преобразования
1 _ 1 * halt
